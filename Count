//Write a recursive methodthat finds the number ofoccurrences of a specified character in a string

public static int count(String s, char c){

  int charCount = 0;
  
  for(int i = 0; i < s.length(); i++)
  {
    if(s.charAt(i) == c)
      charCount++
  }
  
  return charCount;
  
}
